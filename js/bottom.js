document.addEventListener('DOMContentLoaded', function() {
    const box = document.querySelector(".p-box_16")
    console.log('box: ', box);

    // 创建新的签名元素
    const newSignature = document.createElement('div');
    newSignature.className = 'p-image-text_1 flex-row justify-between';
})

